🚀 Quick Start - Biblioteca API & CLI

Se você acabou de clonar este repositório, siga estes passos:

1. Instalar dependências:
npm install

2.Crie um arquivo .env na raiz do projeto:
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=SUA_SENHA_AQUI
DB_NAME=bdb
JWT_SECRET=segredo

3. Criar Banco e Tabelas (Atenção: Isso apaga dados antigos e cria o banco do zero com livros de exemplo):
npm run setup

Admin Padrão: admin@biblioteca.com | Senha: admin123

4. Rodar o Sistema
Você precisará de dois terminais abertos:

Terminal 1 (Servidor):
npm run dev

Terminal 2 (Interface CLI):
npm run cli